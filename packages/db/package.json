{
  "name": "@saas/db",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Prisma Database Client and Schema for PostgreSQL",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts*\""
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "@saas/tsconfig": "*",
    "prisma": "^5.18.0",
    "typescript": "^5.5.4"
  }
}
